
const Heading = ({ text }: { text: string }) => {
    return (
        <header className="text-black text-xl font-semibold tracking-wide">
            {text}
        </header>
    )
}

export default Heading